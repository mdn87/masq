No, package.json declares `"engines": {"node": ">=18"}`. No syntax check found anything Node 18+ specific beyond that declared floor, but the manifest is the constraint that applies — masq isn't tested or supported on Node 16.

Residuals: nothing left open — this was a direct lookup of a declared package requirement, not a runtime compatibility test against Node 16.
