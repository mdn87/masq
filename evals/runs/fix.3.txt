The prior grep hits were just `?.`/`??` (optional chaining/nullish coalescing), which run fine on Node 16 — that's not the constraint here.

**No** — masq declares `"engines": { "node": ">=18" }` in `package.json:17-19`, and its test/scripts pipeline (`npm test`) isn't validated below that. Nothing in the hooks requires Node-18-only APIs (no `structuredClone`, no global `fetch`, no `Array.prototype.at`), so it may well *run* on 16, but it's outside the declared support line — don't rely on that working.
