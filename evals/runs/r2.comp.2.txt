No, package.json declares `"engines": { "node": ">=18" }` (masq/package.json:17-19), so Node 16 isn't supported. The hook code itself doesn't lean on any Node-18-only builtins I found (no `fetch`, `structuredClone`, `node:` imports), so the floor looks like a stated policy rather than a hard runtime dependency — but it's declared, so treat it as the boundary rather than testing your luck on 16.
